GVH_ENABLED=true
# Server-side configuration; /api/auth/config returns only these public project values.
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
# Optional background price checks. Server-only; never use NEXT_PUBLIC for secrets.
SUPABASE_SERVICE_ROLE_KEY=
CRON_SECRET=
PRICE_WATCH_ENABLED=false

# Optional road/walking/cycling distance matrices (server-only).
ORS_API_KEY=

# Optional server-only AI review. Choose a Structured Outputs-compatible Responses model.
OPENAI_API_KEY=
OPENAI_MODEL=
